import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    maskContainer: {
        backgroundColor: 'transparent',
        alignItems: 'center',
        justifyContent: 'center',
    },
    text: {
        color: '#B0B6C4', // Darker standard metallic text
        fontSize: 24,
        fontWeight: 'bold',
    },
});
